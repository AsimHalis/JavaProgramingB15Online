package day62_CollectionsIntro;

public class Collectons_Review {
    /**List(I):Does have index numbers, takes duplicated values
     **Set(I): Does not have index numbers does not take duplicates
     *
     * Queue(I): Does not have index numbers, does accept duplicates
     *         implemented by PriorityQueue
     *         Extended by Deque(I)
     *         Deque(I) is implemented by ArrayDeque(C) and LinkedList(C)
     * *LinkedList(C) is implementing both List(I) and Deque(I)
     *Queue(I):
     *      poll():FIFO==>First In First Out
     *
     */
}
